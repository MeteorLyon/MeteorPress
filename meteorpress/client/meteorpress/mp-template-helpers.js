/*
MP.log('INFO Registering #MeteorPress config helpers', 2);
_.each(MP.config, function (value, key) {
    // Might want to register some methods one day..
    Template.registerHelper('MP' + key, function () { return value; });
});
*/
